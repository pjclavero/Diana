# ESTADO DE EJECUCIÓN

Actualizado por el organizador. Última actualización: **2026-08-04** (barrido de documentación).
Rama `develop` @ `1aa1fbc`.

> **Qué está desplegado y qué no, hoy 2026-08-04.** En la VM 109 corre `develop` @ **`133d760`**
> (commit de despliegue `8220a45`, que documenta la operación). El HEAD de `develop` es
> **`1aa1fbc`** y **NO está desplegado**: desde `133d760` han entrado seis commits
> (`5c3b7ac`, `eb42324` · X-06; `d42f474`, `1aa1fbc` · F6; `85403db`, `4ff69ec` · F4;
> `afe3e82`, `1aa1fbc` · F5) que **sólo existen en el repositorio**. Todo lo que este documento
> marca como «verificado en vivo» se comprobó contra `133d760`, no contra el HEAD.
>
> **Distinción que este documento mantiene siempre, porque confundirla fue el error del
> barrido anterior:** (a) *está en el repositorio* ≠ (b) *está desplegado en la VM 109* ≠
> (c) *está probado de verdad por alguien*. X-06, F4, F5 y F6 están en (a) y en ninguna de
> las otras dos.
>
> **Dos migraciones sin aplicar en producción:** `20260726200000_manager_activation` (F5) y
> `20260726210000_hit_stats_reset` (F4). La base de la VM 109 no las tiene.
>
> **Las tres fases F4, F5 y F6 salieron `NO CONFORME` DOS VECES.** Primera supervisión: diez
> bloqueantes, uno de seguridad (el rol y los permisos salían del token y quedaban obsoletos
> hasta 8 h), corregidos en `4ff69ec` y `1aa1fbc`. **Re-supervisión independiente de esas
> correcciones (2026-08-04): las tres volvieron `NO CONFORME`**, con ocho bloqueantes nuevos y
> un reproche común: los arreglos existían y eran correctos en el código, pero **no estaban
> fijados por ninguna prueba** —doce mutantes sobrevivían, incluida la corrección de
> seguridad, revertible con dos líneas y la suite en verde—. Corregido en `0343a76`, con
> mutación y control neutro en cada tanda: los doce mueren ahora. **Las correcciones de
> `0343a76` NO han pasado revisión independiente**: las tres fases siguen sin poder darse por
> cerradas.
>
> **Estado de las suites, reejecutado hoy 2026-08-04 sobre `36f8e14`**: backend **639 pasan,
> 0 fallan, 7 se saltan** (646; las saltadas necesitan `DATABASE_URL`), frontend **181/181**,
> worker **15/15** (antes no tenía NINGUNA prueba, X-16), simulador **34/34**, contratos
> **43 comprobaciones / 0 fallos**,
> `tsc` limpio **incluido el árbol de pruebas** —que no compilaba— y tres ejecuciones seguidas
> sin intermitencias. Durante el barrido el backend estaba en ROJO (584/8/7 sobre `1aa1fbc`). Las 8 que fallaban estaban todas en
> `test/invitations/manager-activation.spec.ts`: **bomba de relojería en las propias pruebas**,
> no un defecto del producto. El fichero fijaba fechas absolutas (`MANANA = 2026-07-27`) sin
> congelar el reloj, contra un servicio que compara con `Date.now()`, así que **llevaba en rojo
> desde el 28 de julio**. Corregido en `0343a76`: las fechas van atadas al reloj de la
> ejecución, de modo que el resultado no depende del día. *(No se ha podido
> reejecutar con el reloj congelado: no hay `faketime` en esta máquina. La conclusión se
> apoya en la lectura del fichero y del servicio, y en que las 8 fallas son exactamente las
> que usan esas fechas.)* `tsc --noEmit` del backend limpio; `oxlint` con 2 avisos
> preexistentes en backend y 1 en frontend, ninguno nuevo.
>
> **Lo que NO se ha hecho:** nadie ha entrado al panel con credenciales reales para jugar una
> partida de principio a fin. La verificación viva del despliegue fue de superficie (contenedores
> sanos, esquema de la base, códigos de respuesta HTTP de las rutas nuevas). La verificación
> funcional con datos la hará el operador.
>
> **El worker de producción está roto** (hallazgo del supervisor de F4, en la VM 109):
> `diana-worker-1` figura `healthy` mientras **todas** sus tareas fallan en bucle por un
> desajuste del motor de Prisma (`debian-openssl-3.0.x` frente a `1.1.x`). El healthcheck no
> lo detecta porque sólo comprueba que el proceso vive. Por eso la tabla `statistics` está
> vacía allí.

## Paquetes de la Ola 1 (WP-00…WP-12)

> Los paquetes nacieron en ramas `feat/wp0X-*` con worktrees separados. Esas ramas **siguen
> existiendo** (`git worktree list`: 8 worktrees vivos) pero están **congeladas**: desde el
> 2026-07-21 el trabajo se hace directamente sobre `develop` con el método de bloque
> (implementación + tests + supervisor independiente), primero en el programa F1–F6 y después
> en el lote G-A…G-I. La columna «Estado» se refiere al estado del paquete **tal como quedó
> integrado en `develop`**, no al de su rama.

| WP | Paquete | Modelo | Rama | Estado | Evidencia / revisión |
|---|---|---|---|---|---|
| WP-00 | Fundación y contratos | Opus | `develop` | `READY_FOR_REVIEW` (2ª vuelta) | `NO CONFORME` del supervisor; H-01..H-07 corregidos; validador 43/0 con resolución estricta |
| WP-01 | Infraestructura Docker | Sonnet | `feat/wp01-infra` | `READY_FOR_REVIEW` | `docker compose config` limpio en base + 4 perfiles; ACL reescrita tópico a tópico |
| WP-02 | Backend y motor de partidas | Opus | `feat/wp02-backend` → `develop` | `INTEGRADO Y DESPLEGADO` | Integrado en `develop` y desplegado en VM109. A `133d760`: **471 pruebas pasan + 7 saltadas** (las saltadas necesitan `DATABASE_URL`; no están rotas). El paquete siguió creciendo fuera de su rama: F1–F3, G-B…G-I y las deudas se implementaron sobre `develop`. Motor con **5 estrategias** (`random`, `sequence`, `reaction`, `all_against_clock`, `duelo`); `memory` y `no_shoot` siguen en el contrato **sin implementar** |
| WP-03 | Panel web | Sonnet | `feat/wp03-frontend` → `develop` | `READY_FOR_REVIEW` | build + typecheck + lint limpios; 30/30 unit; **E2E ejecutados de verdad con navegador: 18/18** tras corregir 4 bugs reales (strict-mode del badge de conexión, `vite preview` en IPv6, locator por subcadena en responsive, perfil iPad en WebKit no instalado→Chromium). Corre contra adaptador mock (contrato real pendiente, X-06) |
| WP-04 | Firmware ESP32-S3 | Opus | `feat/wp04-firmware` | `CHANGES_REQUESTED` | Esta fila decía «338/338»: era una cifra intermedia. La reproducida ejecutando por calidad el 2026-07-21 es **389/389 comprobaciones en host** (`docs/quality/suites-evidence.md`), y **no se ha vuelto a ejecutar desde entonces**. Sigue en pie la laguna mayor del proyecto: **nunca compilado con ESP-IDF**, nunca corrido en silicio |
| WP-05 | Simulador | Sonnet | `feat/wp05-simulator` | `CHANGES_REQUESTED` | Esta fila decía «28/28»: cifra intermedia. La reproducida ejecutando el 2026-07-21 es **33/33**, sin volver a ejecutarse desde entonces. Conecta al Mosquitto real y completa un escenario; **los impactos no llegaron a persistirse** (X-18-INGESTA) |
| WP-06 | Hardware / KiCad | Opus | `feat/wp06-hardware` | `READY_FOR_REVIEW` | Sin KiCad instalado: **ERC/DRC no ejecutados**. 47 validaciones físicas pendientes |
| WP-07 | CI y pruebas | Sonnet | `feat/wp07-ci` → `develop` | `READY_FOR_REVIEW` | 5 workflows (YAML validado), incl. `firmware-idf.yml` (compila con `espressif/idf:v5.3.1`). Andamiaje `tests/`. Baselines de `develop` reproducidos ejecutando. Los 16 escenarios E2E son **placeholder** (`test.fixme`), documentados en `tests/e2e/README.md`; ninguno implementado |
| WP-08 | VM 109 y despliegue | Sonnet | `feat/wp08-vm-deploy` → `develop` | `DESPLEGADO` | VM 109 aprovisionada y stack en marcha. Despliegues **2026-07-26**: primero el lote (`045fdd1`, commit `8220a45`) y después D9 (`133d760`): 4 migraciones aplicadas contra la base viva, imágenes backend/frontend/worker reconstruidas, **8/8 contenedores `healthy`**. Copia de la base tomada antes de tocar nada. **Sigue sin ejecutarse:** restauración en base aislada y `reboot` con retorno automático. Incidencias vivas de infraestructura: **DNS de la VM roto**, **RAM efectiva muy por debajo de los 4 GB** (ver Bitácora 2026-07-26 y `deployment/procedimiento.md` §8) y —nueva, 2026-08-04— **el worker está roto en producción**: `diana-worker-1` figura `healthy` mientras todas sus tareas fallan en bucle por desajuste del motor de Prisma (`debian-openssl-3.0.x` vs `1.1.x`); el healthcheck sólo comprueba que el proceso vive. **A 2026-08-04 la VM sigue en `133d760`** y le faltan las dos migraciones `20260726200000_manager_activation` y `20260726210000_hit_stats_reset` |
| WP-09 | Documentación `s9-server` | Sonnet | `feat/diana-vm109` | `ENTREGADO, MERGE PENDIENTE` | Según `docs/INFORME-ESTADO-2026-07-21.md` §3: fichas `maquinas/vm109-diana.md` y `servicios/diana.md` + `indice.md`/`inventario.md` actualizados, entregados en el **PR #7** de `s9-server`. **No verificable desde este repositorio** (vive en otro repo): el merge lo hace el operador. La ficha describe el estado a 2026-07-21, anterior al lote G-A…G-I |
| WP-10 | Seguridad | Opus | informe | `READY_FOR_REVIEW` | `docs/security/threat-model.md` + 6 ficheros en `evidence/` (salida real de comandos). 18 hallazgos F-01..F-18: 1 crítico (F-02, ACL MQTT por `client_id`), 6 altos, 10 medios, 1 bajo. **8 de ellos NO reproducidos en ejecución**: el stack no arranca (F-13, faltan `Dockerfile` de backend y worker) |
| WP-11 | Calidad | Opus | informe | `READY_FOR_REVIEW` | **CONFORME CON OBSERVACIONES** (`docs/quality/dictamen-calidad.md`). Reproducidas ejecutando: contratos 43/0, firmware 389/389, simulador 33/33, backend 157+5 saltados (saltados por falta de `DATABASE_URL`, no rotos), frontend typecheck/build/lint limpios + 30/30. Salidas en `docs/quality/suites-evidence.md`. Observaciones: matriz apunta 46× a `docs/testing/` **vacío**; 16 E2E del §19 son `test.fixme` (0 aserciones); §2.4 implementa+prueba mismo paquete en backend/firmware/simulador; Dockerfile de backend/worker YA existen (X-10/F-13 desactualizado); `worker/{tasks,main}.ts` sin prueba |
| WP-12 | Supervisión | Opus | dictamen | `IN_PROGRESS` | Ola 0 dictaminada `NO CONFORME` (`SUPERVISOR_REPORT.md`, 2026-07-20); la **2ª vuelta sobre lo corregido y el dictamen final del programa siguen sin emitirse**. Lo que sí existe es supervisión **por bloque**, con supervisor independiente en cada uno de F1–F3 y G-A…G-I (dictámenes en las tablas de abajo). La 4ª supervisión de D9 se emitió: **CONFORME CON OBSERVACIONES**. **A 2026-08-04 el recuento de rondas `NO CONFORME` reales sube a nueve:** G-G 1ª; D9 1ª/2ª/3ª; **X-06 1ª; F4 1ª; F5 1ª; F6 1ª** — y las correcciones de X-06 sí pasaron una supervisión, pero **las de F4, F5 y F6 están pendientes de revisión independiente al escribir esto** |

## Hallazgos abiertos que cruzan paquetes

> **Aviso de nomenclatura (resuelto 2026-07-26).** El identificador `X-18` se usó con **dos
> sentidos distintos**: en esta tabla es la falta de independencia §2.4 de las suites unitarias;
> en `deployment/procedimiento.md` §9, `INFORME-ESTADO-2026-07-21.md` y el §6 de producto es
> **la ingesta e2e no verificada** (el simulador publica y no se persisten impactos). Para no
> romper las referencias históricas se conservan ambos: el segundo pasa a llamarse
> **`X-18-INGESTA`** y tiene fila propia abajo. Ambos siguen **abiertos**.

| # | Hallazgo | Origen | Afecta a | Estado |
|---|---|---|---|---|
| X-01 | El presupuesto de GPIO del dosier §8.4 **no cuadra**: 29 pines necesarios (34 con reserva) frente a 25 disponibles | WP-06 | WP-04, WP-06 | Abierto. Topología alternativa propuesta (OR de diodos + 74HC165 + ADC SPI): 21 usados, pero 3 de los 4 de reserva son JTAG |
| X-02 | Térmica del convertidor: Tj = 137 °C a brillo máximo con η=0,90, por encima del límite de 125 °C | WP-06 | WP-06 | Abierto. El tope de brillo pasa de recomendación a **requisito térmico** |
| X-03 | El detector de envolvente pasivo del dosier §9.2 tiene τ_ataque de 3,20 ms para impactos de <1 ms: inservible | WP-06 | WP-06 | Corregido en el diseño con seguidor (4,70 µs); pendiente de validación física |
| X-04 | GPIO 3 propuesto para sensado de 12 V es pin de strapping | WP-04 | WP-04, WP-06 | Abierto, a resolver en la revisión de pinout |
| X-05 | Healthchecks del stack asumen `/health` en backend, worker y frontend | WP-01 | WP-01, WP-02, WP-03 | Abierto, se verifica al integrar |
| X-06 | El contrato que asume el panel no está negociado con el backend. Nació como «contrato REST»; la parte REST se ha ido cerrando (X-21) y **lo que queda vivo bajo este número es el contrato WebSocket**: el panel abre un WebSocket **crudo** contra `${VITE_WS_URL}/games/:id/live` (`server/frontend/src/api/realGameSocket.ts:41`), mientras el backend sirve **socket.io** en `path: '/ws/socket.io'`, `namespace: '/live'` (`server/backend/src/modules/websocket/live.gateway.ts:39-42`). Un `WebSocket` nativo no habla el protocolo de socket.io, así que **la vista en directo no puede funcionar contra el backend real**, y el proxy sólo enruta `/ws/` → `backend:3000/ws/` (`infrastructure/proxy/nginx.conf:103`) | WP-03 | WP-02, WP-03 | **CORREGIDO EN CÓDIGO (`5c3b7ac` + `eb42324`), SIN DESPLEGAR Y SIN PROBAR CON NAVEGADOR REAL.** `5c3b7ac`: gateway con `path: '/ws/socket.io'`, salas reales con `subscribe_game`/`unsubscribe_game`, último `game/state` recordado por partida con cota dura de 200, y el panel a `socket.io-client`. Ni `LiveGateway` ni `realGameSocket` tenían **ninguna** prueba: añadidas 15 + 13. **1ª supervisión: `NO CONFORME`, 2 bloqueantes reales; corregidos en `eb42324`.** El supervisor montó nginx 1.26 de verdad con el fichero del repo y probó el transporte de extremo a extremo: eso sí funcionaba. Lo que no: **B1 · el canal no pedía credenciales y difundía TODO** — el arreglo por salas sólo cubría el evento `live`, y el «canal de diagnóstico» seguía haciendo `server.emit` a todo el namespace, así que un cliente **sin token y sin suscribirse a nada** recibía la manguera MQTT completa (estados de partidas ajenas, telemetría, impactos, presencia); *el commit anterior afirmaba haber corregido esto y la afirmación era falsa para ese canal*. Ahora el saludo exige un JWT válido (los guards globales son de contexto HTTP y no llegan al gateway) y el diagnóstico va a una sala que hay que pedir expresamente. **B2 · el panel reventaba con un `game/state` válido** (`active_targets` no está en `required` y `LiveGamePage` lo recorría: `TypeError: … is not iterable`); antes era inalcanzable porque nunca conectaba, y el arreglo lo hizo alcanzable. Observaciones corregidas: los dos extremos usaban **palabras distintas para lo mismo** (`aborted` frente a `finished`/`cancelled`, `all_against_clock`/`all_vs_clock`, `penalty_applied`/`penalty`), así que una partida abortada se veía como si siguiera corriendo para siempre → nuevo `liveContract.ts` que traduce en la frontera; la cota LRU desalojaba **la partida en curso** y conservaba las terminadas; CORS del gateway alineado con `CORS_ORIGINS`; nginx mandaba `Connection: upgrade` también en las peticiones de sondeo (añadido el `map $http_upgrade $connection_upgrade`, validado con `nginx -t`). **HALLAZGO GRAVE que sigue abierto:** el panel de producción **se compila en modo `mock`** (`server/frontend/Dockerfile:19` y `compose.yml:83`, `VITE_API_MODE: ${VITE_API_MODE:-mock}`), así que **ni desplegando esto se usaría el cliente nuevo**; además `compose.yml` exportaba `VITE_WS_BASE_URL` como variable de ejecución cuando el código lee `VITE_WS_URL` en COMPILACIÓN (no hacía nada). El cableado se corrigió, pero **el modo sigue en `mock` a propósito**: pasar a `real` exige cerrar antes X-21. **Lo que sigue abierto:** no probado con navegador real ni contra MQTT/firmware real, y `io server disconnect` deja al cliente sin reintento (riesgo latente; hoy nadie lo emite). G-I **no** lo cerró, pese a que el backlog decía «G-I cierra X-06/X-18» |
| X-18-INGESTA | **La ingesta e2e nunca se ha verificado:** tras ejecutar el escenario del simulador contra el broker de la VM, `hit_events = 0` y sin trazas de ingesta en el backend (`deployment/procedimiento.md` §9). Antes se le llamaba «X-18» a secas, chocando con el X-18 de §2.4 | WP-08/WP-11 | WP-02, WP-05 | **Abierto.** Sin reintentar desde 2026-07-21. G-I añadió persistencia de **presencia** y atribución de impactos, pero **no** re-ejecutó la prueba de ingesta extremo a extremo |
| X-07 | ~~E2E de Playwright no ejecutables aquí~~ **Reclasificado por WP-07: Chromium SÍ se instala.** Ejecutados de verdad: **6 pasan, 12 fallan.** No es utillaje: bug real de strict-mode en `server/frontend/e2e/game-flow.spec.ts:46` (`getByText(/en directo\|conectando/i)` casa 2 elementos) y `vite preview` atado sólo a IPv6 `::1` (falta `--host 127.0.0.1` en `server/frontend/playwright.config.ts`) | WP-03 | WP-03, WP-11 | **Cerrado.** WP-03 los corrigió (4 bugs en total, no 2); E2E 18/18 con navegador real. El adaptador es mock: la ejecución contra el backend real queda en X-06 |
| X-16 | El worker (`server/worker`) **no tiene ningún test**: `npm test` sale con código 1 ("No tests found"). En CI se usa `jest --passWithNoTests` para no romper, pero es una laguna de cobertura, no una solución | WP-07 | WP-02 | **PARCIALMENTE CERRADO (2026-08-04, `36f8e14`).** El worker ya tiene banco de pruebas propio (`jest.config.js` + `src/health.spec.ts`): **15 pruebas** sobre la lógica de salud, verificadas por mutación con control neutro. **Sigue abierto** lo que señala X-20: `tasks.ts` (ejecución de tareas) y `main.ts` (arranque) no tienen prueba propia —`tasks.ts` sí se ejerce desde `server/backend/test/worker/statistics-recompute.spec.ts`, añadido en F4—. Y el arreglo del motor de Prisma **no es comprobable con una prueba unitaria**: es una decisión de Dockerfile/esquema, y se verifica inspeccionando el artefacto tras reconstruir |
| X-08 | **F-02** · La ACL de MQTT autoriza por `%c` (`client_id`, que elige el cliente) y no por `%u`: unas credenciales cualesquiera de módulo bastan para publicar `hit`, `status`, `telemetry`… en nombre de **cualquier otro módulo**. Falta `use_username_as_clientid true` | WP-10 | WP-01, WP-04, WP-05 | **CONFIRMADO EN VIVO (2026-07-21)** contra el broker de la VM: credenciales de m1 con `client_id=m2` publicaron en el tópico de m2 y el backend lo recibió. Mitigación = alinear usuario=client_id=module_id + `use_username_as_clientid true` (cambio de contrato §8, para el supervisor) |
| X-09 | **F-04** · El contrato de variables de entorno entre `compose.yml` y el backend está roto en 5 variables: `JWT_SECRET` no se pasa, `CORS_ORIGIN` vs `CORS_ORIGINS`, `MQTT_HOST/PORT` vs `MQTT_URL`, `SESSION_SECRET` sin uso, `DIANA_ADMIN_*` sin pasar. Con `NODE_ENV=production` el backend **no arranca**; sin él, firma los JWT con `'desarrollo-inseguro-cambiar'` | WP-10 | WP-01, WP-02, WP-08 | Abierto. Alto |
| X-10 | **F-13** · Faltan los `Dockerfile` de `server/backend` y `server/worker`; 4 de los 6 servicios que construyen imagen apuntan a un contexto sin `Dockerfile`. Impide ejecutar el stack y, por tanto, verificar F-02, F-03, F-05, F-06, F-08, F-09, F-11 y F-12 | WP-10 | WP-01, WP-02, **WP-08 (en curso)** | En curso en WP-08. WP-10 debe reabrir la verificación cuando arranque |
| X-11 | **F-07** · Sin TLS en ninguna capa: el bloque HTTPS de `nginx.conf:136-144` y el listener MQTT 8883 de `mosquitto.conf:57-61` están comentados. JWT, contraseñas de login y credenciales MQTT viajan en claro por la LAN | WP-10 | WP-01, WP-04, WP-08 | Abierto. Alto |
| X-12 | **F-08** · El proxy reescribe `/api/` → `/` pese al prefijo global `api` del backend, y `/ws/` no casa con el namespace `/live`. Al corregir el enrutado hay que verificar que `/api/auth/login` sigue en la zona `api_auth` (5 r/s) y no cae en la general (20 r/s) | WP-10 | WP-01, WP-02, WP-07 | Abierto. Riesgo de perder el control antifuerza bruta al arreglar el fallo funcional |
| X-13 | **F-14 + F-02** · Sin secure boot ni cifrado de flash, el acceso físico a un módulo entrega sus credenciales MQTT. Aceptar ese riesgo por custodia física **sólo es defendible si F-02 se cierra antes**: mientras la ACL autorice por `client_id`, comprometer un módulo compromete a todos | WP-10 | WP-01, WP-04, WP-06 | Abierto. Decisión conjunta pendiente |
| X-14 | **F-16** · El firmware acepta comandos sin verificar caducidad cuando no hay hora (`clock_ok == false`). Requiere (a) rechazar los comandos con consecuencia física en ese estado y (b) confirmar que existe servidor NTP alcanzable en la LAN, que no tiene salida a Internet | WP-10 | WP-04, WP-08 | Abierto |
| X-15 | **F-17** · `npm audit`: 23 vulnerabilidades en backend (12 altas), 3 altas en worker (`effect`/`prisma`, la única con camino de ejecución en producción), 5 en simuladores (1 crítica sin identificar: falta `npm audit --json`) | WP-10 | WP-02, WP-05, WP-07 | Abierto |
| X-17 | La matriz `TEST_MATRIX.md` remite **46 veces** a `` `docs/testing/` `` como lugar de la evidencia, pero ese directorio está **vacío** (sólo `.gitkeep.md`). Los 46 punteros están colgados; la evidencia real vive en las suites y en `docs/security/`. En concreto, E2E §19 (E-01..E-16) e Infra/Carga (I-*, L-*) se dan por cubiertos y no lo están (E2E son placeholder; Infra/Carga necesitan Docker vivo) | WP-11 | WP-07, WP-02 | Abierto. Verificado ejecutando |
| X-18 | **§2.4** · Las tres suites unitarias verdes (backend 157, firmware 389, simulador 33) las escribió el mismo paquete que escribió el código (`OWNERSHIP.md`: `server/backend/**`, `firmware/**`, `simulators/**` incluyen su `test/`). La única capa de prueba de propietario distinto (E2E de WP-07, `tests/e2e/`) está al **0 %**: 16 `test.fixme`, 0 aserciones. Implementa→prueba recae en un solo paquete; no hay revisión independiente por función | WP-11 | WP-02, WP-04, WP-05, WP-07 | Abierto |
| X-19 | X-10/F-13 **desactualizado**: `server/backend/Dockerfile` y `server/worker/Dockerfile` YA existen en `develop`. La verificación de F-02/F-03/F-05/F-06/F-08/F-09/F-11/F-12 que WP-10 dejó bloqueada por su ausencia debe reabrirse contra el stack | WP-11 | WP-08, WP-10 | Abierto |
| X-20 | Matiz a X-16: el worker no está del todo sin prueba. Su lógica pura de planificación (`worker/src/schedule.ts`) SÍ se ejerce desde `server/backend/test/worker/schedule.spec.ts` (verde). Lo que queda sin prueba es `worker/src/tasks.ts` (ejecución de tareas) y `main.ts` (arranque) | WP-11 | WP-02 | Abierto |
| X-22 | **El panel no implementa autenticación pese a que el backend la exige.** El backend tiene JWT + RBAC (5 roles) con guards **globales** (`app.module.ts`: `JwtAuthGuard`+`PermissionsGuard`): todo endpoint no-`@Public` requiere `Authorization: Bearer`. El frontend NO tiene login: `grep -rniE 'login/token/authorization/bearer' server/frontend/src` → 0 resultados; `realAdapter.ts` no añade cabecera de auth; `UsersPage` llama a `apiClient.listUsers()` sin token. Consecuencia: **contra el backend real toda llamada del panel daría 401**, agravando X-21. Falta: pantalla de login, almacenamiento/envío del JWT, guarda de rutas por rol y siembra del admin inicial (`DIANA_ADMIN_*`, ligado a F-04). Va en el mismo bloque que X-21 | WP-11 | WP-02, WP-03, WP-10 | **CERRADO por F1** (desplegado y verificado en VM109). Comprobado en el código a `133d760`: existe `src/pages/login`, `src/api/auth/tokenStore.ts` guarda el JWT y **todos** los clientes de API nuevos (`modulesApi`, `topologyApi`, `playersApi`, `presetsApi`, `viewsApi`, `scoreboardApi`, `resilienceApi`, `participantsApi`, `firmwareApi`, `invitationsApi`) añaden `Authorization: Bearer`. La siembra del admin inicial se resolvió y el operador cambió la contraseña |
| X-21-CORR | **CORRECCIÓN de X-21 (2026-07-21).** X-21 afirmaba que `/systems`, `/modules`, `/players`, `/teams`, `/topology`, `/firmware`, `/presets` **no existían** en el backend. **Era FALSO**: sí existen, generados por el CRUD por fábrica `common/crud/crud.controller.ts` (`@Controller(options.path)` dinámico) y registrados en `app.module.ts` (SystemsModule, ModulesModule, PlayersModule, TeamsModule, TopologyModule, FirmwareModule, PresetsModule). El grep de `@Controller('literal')` no los vio. **Verificado en vivo (VM109):** `GET /api/{systems,modules,players,teams,topology,firmware,presets}` → **HTTP 200** con token admin. La divergencia REAL es menor de lo dicho: (1) el frontend usaba `/api/v1` (ya corregido a `/api` en F1) y (2) **forma de respuesta**: el CRUD devuelve `{items:[...]}` con entidades crudas, mientras el panel espera DTOs concretos (SystemStatus, ModuleStatus con telemetría anidada, Topology con slots…). Conectar el panel a real = **adaptar la forma en el `realAdapter`**, no construir endpoints. | Organizador | WP-02, WP-03 | Verificado ejecutando. Corrige X-21 |
| X-21 | **Divergencia de contrato REST panel↔backend.** El panel de producción se compila en modo real (`server/frontend/Dockerfile` → `ARG VITE_API_MODE=real`, base `/api/v1`), pero `server/frontend/src/api/realAdapter.ts` llama a recursos que el backend **no expone**: base `/api/v1` (el backend usa prefijo `api` sin `v1`) y rutas `/systems`, `/systems/:id/modules`, `/modules/:id/config`, `/topology`, `/players`, `/teams`, `listResults→/games?phase=finished`. Verificado ejecutando: `grep @Controller('systems\|modules\|results\|players\|teams\|topology')` → NINGUNO; los controllers reales son `health, roles, users, statistics, exports, mqtt, accuracy, audit, ammo, hits, maintenance, auth, games`. Consecuencia: contra el backend desplegado, Estadísticas y las pantallas de configuración devolverían 404 («No se ha encontrado el recurso»). El E2E 18/18 verde corre contra el adaptador **mock**, no contra el backend real. Más amplio que X-06 (WS) y X-18 (ingesta): es el contrato REST completo del panel. **Decisión de rumbo:** armonizar `realAdapter` a las rutas reales del backend, o implementar en el backend los recursos que el panel espera (`/systems`, `/modules`, `/players`, `/teams`, `/topology`) | WP-11 | WP-02, WP-03 | **PARCIALMENTE CERRADO — sigue abierto para las pantallas heredadas.** Cerrado: las pantallas nuevas (módulos, topología por panel, jugadores, equipos, presets, vistas, participantes, marcador, resiliencia, firmware, invitaciones) NO usan `realAdapter`: tienen su propio cliente contra `/api` real con JWT, y `VITE_API_BASE_URL` ya es `/api` (sin `v1`). Además el `Dockerfile` del frontend **ya no compila en modo real**: `ARG VITE_API_MODE=mock` (`server/frontend/Dockerfile:19`), así que lo que sigue colgando de `realAdapter` muestra **datos de demostración** y `AppShell` lo declara (`DEMO_DATA`). Abierto, pero **menos que antes**: el 2026-07-26 esta casilla decía que el backend no expone `/modules/:id/commands/identify` ni `/modules/:id/targets/:i/{calibrate,test-sensor,test-led}` ni `/modules/:id/diagnostics`, y **eso ya no es cierto** — F6 (`d42f474`) los implementó y hoy existen en `module-diagnostics.controller.ts` (verificado leyendo el fichero). **Sigue sin exponer:** `/modules/:id/telemetry`, `/systems/:id/topology`, `/incidents` y el `/diagnostics` global sin módulo. **Y sigue en pie el motivo de fondo:** aunque las rutas de diagnóstico ya existan, la imagen de producción se compila con `VITE_API_MODE=mock` (`server/frontend/Dockerfile:19`), así que **lo que se despliegue seguirá enseñando datos de demostración** hasta que se pase a `real`; y pasar a `real` exige antes armonizar las pantallas heredadas que aún cuelgan de `realAdapter`. Es decir: F6 dejó de ser un problema de «el backend no lo tiene» y pasó a ser un problema de «el panel de producción no lo pide» |

## Lote de mejoras del panel (G-A…G-I) — 2026-07-22

> **DESPLEGADO EN VM109 el 2026-07-26** (`develop` @ `045fdd1`; después D9 @ `133d760`): 4 migraciones aplicadas
> (`matrix_layouts`, `participant_panel`, `module_board_and_deploy_guard`,
> `module_offline_since`), imágenes backend/frontend/worker reconstruidas, 8/8 contenedores
> `healthy`, panel 200 y API 200. Verificado en vivo: las 6 rutas nuevas responden 401
> (existen y exigen auth; una ruta inexistente da 404) y el esquema real tiene la tabla, las
> 3 columnas y el índice parcial de OTA. **N8 CERRADO CON EVIDENCIA:** la prueba de
> concurrencia del cerrojo de panel se ejecutó por fin contra PostgreSQL real (base de
> pruebas aislada) — dos `start` simultáneos, **gana exactamente uno**, el otro recibe 409 y
> la perdedora no queda marcada ni con la ronda arrancada. Integración 7/7.
> Incidencia preexistente encontrada: **la VM no resolvía DNS** (MagicDNS de Tailscale roto);
> ver `docs/deployment/procedimiento.md` §8. Sin autenticar contra la API con credenciales
> reales: la verificación funcional con datos queda pendiente del operador.
>
> **Lo que quedó FUERA de ese despliegue:** el commit `133d760` (D9, barrido de obsolescencia
> de G-I) es **posterior** al despliegue. El código del barrido está en `develop` y probado en
> local, pero **no corre en la VM**. Mientras no se redespliegue, la detección de caída que hay
> en producción es la que depende del Last Will, con el agujero que D9 describe.

Origen: conversación de dirección 2026-07-22. Especificación: `docs/product/alcance-panel-roles-firmware.md` §6. Método por bloque: implementación + tests + supervisor independiente.

| Bloque | Alcance | Estado |
|---|---|---|
| **G-A** | Quick wins UX: toggle/apagar LED, botón volver, fix drag editor de matriz | ✅ **CONFORME CON OBSERVACIONES** (supervisor reejecutó: 55 tests, tsc/lint/build limpios). Toggle LED (mismo botón enciende/apaga `off`) + "Apagar todas"; `BackButton` en sensores/LED/calibración/detalle; editor de matriz: soltar en celda ocupada **intercambia** (swap, era el bug de la central) + realce destino + celda bloqueada no finge soltable. Defecto latente del supervisor **corregido**: `applyMove` no machaca al ocupante si el módulo viene de fuera (hoy inalcanzable; blinda para `listModules()`). Frontend 57. Desplegada en VM109 |
| G-B | Firmware: subir el binario y servirlo por HTTP local (cierra ciclo OTA) | ✅ **CONFORME CON OBSERVACIONES** (supervisor reejecutó: backend 198, frontend 58, tsc/lint/build limpios; verificó en vivo sha256 byte-a-byte, 401/403/409/400/404, path-traversal 400). `POST /api/firmware/upload` (admin multipart, sha256/tamaño del servidor) + `GET /api/firmware/:id/binary` (`@Public`, lo descarga el módulo). Volumen `diana_firmware`. Observaciones corregidas: OBS-1 (borrar versión limpia el binario), OBS-2 (límite interceptor=config), OBS-3 (tests unlink). Desplegado VM109; env `FIRMWARE_PUBLIC_BASE_URL=http://192.168.1.209:8080` |
| G-C | Dashboard de módulos: resumen paginado + panel del módulo (Ver 9 dianas/Calibración/Pruebas/Actualizar) | ✅ **CONFORME CON OBSERVACIONES** (supervisor reejecutó: backend 203, frontend 61, tsc/lint/build limpios; verificó en vivo overview 200, scoping por rol estricto, sin N+1, colisión de rutas descartada). Observaciones no bloqueantes documentadas: OBS-1 `updateAvailable` compara versión por texto no semver (conforme a §6.6; endurecer con D3), OBS-2 fw null→update (ok), OBS-3 500 sólo con `sub` no-UUID sintético (inalcanzable en prod). Backend `GET /api/modules/overview` (`ModulesOverviewService`; admin todos, gestor los suyos) con summary + `updateAvailable`. Frontend `ModulesPage` real: resumen, paginación 9/pág, expansión en la misma ventana con acciones + «Actualizar → vX». Verificado en vivo: `demo-diana-01` fw 1.0.0 → updateAvailable 1.2.0. Backend 203, frontend 61. **Incidencia:** build de Docker sirvió `dist` viejo (500 en overview) → resuelto con `build --no-cache`; documentado en procedimiento §8 |
| G-D | Jugadores+equipos (F4/F5): buscar, invitación correo+SMTP, QR, temporales, equipos | ✅ **CERRADO Y DESPLEGADO** (el lote se desplegó el 2026-07-22 y de nuevo el 2026-07-26). **G-D.1 ✅ CONFORME CON OBSERVACIONES (supervisor; OBS-1 take-NaN corregida).** Backend `PlayersSearchService` + `GET /api/players/search?q=` (nombre/apellidos/licencia/usuario; `registered`). Frontend `PlayersPage` real (buscar, alta plantilla, registrado/plantilla, cambiar equipo por fila) + `TeamsPage` real (listar/crear). Crear/PATCH ya eran CRUD. Backend 232, frontend 80. **G-D.2 (temporales) ✅ CONFORME CON OBSERVACIONES (supervisor; garantía anti-estadística confirmada estructural; slot-race P2002→409 corregido con reintento):** `Participant.guestName` (migración `20260722180000_participant_guest`); `ParticipantsService` de negocio (identidad XOR: playerId O guestName; temporal sin Player→sin estadística, estructural; slot auto); `/api/participants` GET/POST/DELETE; `ParticipantsPage` (elige partida, añade temporal por nombre o registrado buscando, lista marcada). Backend 241, frontend 83. **+ reasignar equipos a la partida** (PATCH /participants/:id/team). **+ QR para unirse** (`Game.joinCode`, `GameJoinService`, `/games/join/:code` público, página `/unirse/:code`, react-qr-code). **+ invitación por correo + panel SMTP** (`Invitation`+`SmtpSetting`, mailer honesto, código visible/auditado, `/invitacion/:code` público, aceptar→Player registrado; SMTP admin-only). **G-D COMPLETO.** Backend 269, frontend 90. **✅ TODO EL LOTE DESPLEGADO Y VERIFICADO EN VIVO EN VM109 (2026-07-22):** 4 migraciones aplicadas, modos re-sembrados (duelo incluido), 8 contenedores sanos, endpoints nuevos 200, públicos 404 correcto, invitación creada con código, panel 200. **Supervisor final del lote ✅ CONFORME** (rutas públicas sin fugas, password SMTP nunca devuelta, smtp:* solo admin, sin defectos bloqueantes; obs menor: `assertEqualSetup` sin cablear hasta el flujo real de duelo multi-panel de G-H) |
| G-E | Modos nuevos: duelo + demo | ✅ **CERRADO Y DESPLEGADO** (2026-07-26, con el lote). **Demo CONFORME** (desplegado ya el 2026-07-22). **Duelo: supervisor base CONFORME CON OBSERVACIONES**; su única observación abierta —`assertEqualSetup` sin cablear— la cerró G-H.1 con `dueloReadiness`. Motor: `DueloModeStrategy` (secuencia compartida por semilla = mismo patrón a cada jugador sobre sus módulos; carrera orden estricto) + `rankDuelo` puro (más aciertos, menos tiempo; empates comparten posición). El motor ya es por-competidor → duelo = mismo plan por jugador + ranking, sin reescribir núcleo. Panel: `DueloPage` **1vs1 A LA VEZ (simultáneo, por defecto)** con dos rejillas en paralelo y misma secuencia; la ronda termina al primer completado (gana con más aciertos + menos tiempo); + modo por turnos. Control `assertEqualSetup` (mismos elementos). Supervisor base CONFORME C/OBS (OBS-1 «más aciertos inerte» resuelta con el fin-al-primer-completado). Backend 228, frontend 76. Modo demo (§6.4): frontend puro `/demo` — 12 dianas aleatorias, impacto tocando la encendida (con hardware sería el evento real), cronómetro, repite al relanzar; efímero (10 últimos tiempos en sessionStorage, nada a BD). Lógica pura testeada. Frontend 71. Desplegado VM109 (bundle nuevo, panel 200). **Duelo** aparte: no es una estrategia más, toca la puntuación por jugador (participantes/resultados) → bloque propio con decisión de producto |
| G-F | Presets por gestor (5 custom) | ✅ **CONFORME CON OBSERVACIONES** (supervisor reejecutó: backend 214, frontend 64, tsc/lint/build limpios; verificó en vivo crear/listar/borrar, duplicado→400, scoping gestor). Observaciones atendidas en código (a desplegar con G-E): OBS-2 FK P2003→400, OBS-3 unicidad de muestra en `update` + tests de PATCH (backend 217). OBS-1 (carrera límite/unicidad sin transacción) diferida (misma clase que F2/F3). **Método corregido:** verificación viva de supervisores = SOLO LECTURA; escrituras (con limpieza) las hace el organizador. `GamePreset` gana `owner_id` (migración additiva; `name` único por dueño). `PresetsService` con reglas: gestor los suyos (máx 5) + ve muestra; admin todos + crea muestra. Modo por clave. `/api/presets` propio. Frontend `PresetsPage` (X/5, alta modo+config, borrado). Backend 213, frontend 64. Verificado en vivo: crear/listar/borrar, duplicado→400 (dueño e muestra), modo inexistente→400. **Hallazgos:** BuildKit OOM en la VM (~1GB) dejaba imágenes viejas → build con RAM liberada (procedimiento §8); `game_modes` sin sembrar en prod → `seed:reference` compilado (procedimiento §8) |

| G-G | Dashboard resultados/estadísticas estilo «máquina de dardos» | ✅ **CONFORME CON OBSERVACIONES** (2ª supervisión sobre `b40dbbb`; 1ª ronda fue **NO CONFORME** por defecto bloqueante real: nadie escribe `HitEvent.participantId`, así que el marcador en vivo mostraba «0 aciertos» para todos junto al recuento real de impactos. Corregido: los impactos sin dueño se DECLARAN («sin atribuir», sin posición, aviso visible); con un solo jugador se deduce y se marca **«deducido»**, no como medida (O1); rejilla multipanel identifica el panel de cada módulo (O2)). **DESPLEGADO el 2026-07-26** con el lote (el «SIN DESPLEGAR» anterior era del 2026-07-26 por la mañana, antes del despliegue). Dominio puro `src/domain/scoreboard/scoreboard.ts`: `buildRanking` (manda el `Result` consolidado; si no lo hay, cuenta impactos y marca `provisional`) y `buildBoard` (estado por diana: acertada / impacto no válido / pendiente). Criterio de orden reutilizado del duelo (más aciertos válidos; a igualdad, menor tiempo; empates comparten posición). `GET /api/scoreboard/games/:gameId[?round_id=]` y `GET /api/scoreboard/participants/:id` (histórico; el **temporal se declara sin histórico**, no con ceros). Frontend `/marcador[/:gameId]`: clasificación, rejilla de dianas por módulo con su posición en la matriz, ficha del jugador y refresco automático **sólo mientras la partida está viva**. Invariante de honestidad: precisión no calculable se muestra como «no calculable», nunca 0 %. Backend 328, frontend 104 |
| G-H | Matriz avanzada: favoritas, paginación por panel, concurrencia (View) | ✅ **CERRADO Y DESPLEGADO (2026-07-26).** Recorrido de supervisión de G-H.2: 1ª supervisión **CONFORME CON OBSERVACIONES** (D1-D10 corregidos en `6549c94`: cerrojo consultivo por panel dentro de la transacción, `control()` validando transición, borrado de vista con partida encima, coordenadas acotadas); 2ª supervisión, observaciones **N1-N8** corregidas (`bb45035`), entre ellas que `start()` resucitaba una partida `finished`/`aborted` y que la orden MQTT se publicaba fuera de la transacción. **N8 estaba declarado como «prueba escrita pero NO ejecutada» y se cerró con evidencia real en el despliegue** (dos `start` concurrentes contra PostgreSQL real: gana exactamente uno, el otro 409; el primer intento falló y destapó un defecto de la propia prueba). **D9 del supervisor de G-H** (paneles sin acotar por dueño) se cerró en el bloque de deudas (`e4a9de3`). — **G-H.1 ✅ CONFORME CON OBSERVACIONES (supervisor; D1 scope-leak get/duelo-readiness + D2 addPanel-race corregidos), DESPLEGADO Y VERIFICADO en VM109.** Entidad `View` (Opción B): agrupa paneles; `Game.viewId` opcional; migración `20260722210000_views`. `ViewsService` (scoping propias+públicas, add/remove panel, **`dueloReadiness` cablea `assertEqualSetup`** → cierra la observación del duelo). `/api/views` + `ViewsPage`. Verificado en vivo: /views 200, duelo-readiness operativo. Backend 280, frontend 92. **G-H.2 (resto) ✅ supervisado (dos vueltas, correcciones aplicadas) y DESPLEGADO el 2026-07-26:** (a) **guardarraíl «un juego activo por panel»** — `assertPanelsFree` en `GamesService.start()` (ocupan panel `armed\|running\|paused`; cubre partidas sobre vista multi-panel y partidas de otra vista que compartan panel) + `GET /api/games/panel-occupancy`; (b) **matrices favoritas** — modelo `MatrixLayout` (migración aditiva `20260726100000_matrix_layouts`), captura de la colocación de un panel, aplicar **por slug** avisando de los módulos ausentes, favoritas, máx. 20 por dueño, scoping por dueño; (c) **selector de panel + datos reales (cierra X-21 *para el editor de matrices*, no para las pantallas que siguen colgando de `realAdapter`)** — `GET/PUT /api/topology/panels[/:idOrSlug]` con la matriz REAL del panel; el editor deja de leer datos simulados, muestra la bolsa de módulos sin colocar y gestiona las favoritas. Backend 309, frontend 98 (recuentos del momento; a `133d760` el backend va por 471) |
| G-I | Resiliencia y reconexión (auto-pausa módulo/coordinador). *(El backlog decía «cierra X-06/X-18»; **no los cierra**: X-06 es el contrato WebSocket y sigue abierto, y la ingesta e2e —X-18-INGESTA— no se ha vuelto a ejecutar.)* | ✅ **BLOQUE COMPLETO DESPLEGADO Y DICTAMINADO (2026-07-26).** Cuerpo del bloque desplegado en `045fdd1`; D9 (barrido de obsolescencia) desplegado en **`133d760`** — verificado por SSH: `git log` de la VM en `133d760` y la imagen viva contiene `forgetBlackout` y `BLACKOUT_GRACE_MS`. **4ª supervisión: CONFORME CON OBSERVACIONES**, sin bloqueantes nuevos; observaciones corregidas en `70b2590`. **Hallazgo de partida:** la presencia MQTT (`module/+/presence`, con LWT) se validaba y se DESCARTABA — `Module.online` no lo escribía nadie, así que ninguna caída se detectaba. Ahora: puerto `PRESENCE_SINK` con persistencia real (online, `lastSeenAt`, `offlineSince`, boot_id, firmware, ip, mac); dominio puro `decidePresenceChange` (módulo implicado → auto-pausa; **coordinador de la partida** → pausa dura; ajeno → sólo registro; vuelta → NO se reanuda solo) y `reconnectCountdown`. `GET /api/resilience/games/:id` + `POST .../decision` (`resume`, `resume_without`, `abort`) y `ResiliencePanel` en la partida en directo. **Bloqueantes corregidos:** D1 ronda congelada sin salida al reconectar el módulo (ahora hay «Reanudar» y el aviso no desaparece); D2 `decide()` resucitaba partidas cerradas saltándose el guardarraíl de panel de G-H (ahora valida estado + panel libre); D3 el fallo de publicación era indetectable — `publish()` devolvía void y mqtt.js encola en vez de lanzar (ahora `sendSystemCommand` informa de `delivered` y la pantalla lo dice); D4 en vistas multipanel `statusOf` sólo miraba el panel principal. **Observaciones corregidas:** D5 dos caídas simultáneas publicaban dos `pause_game` (transición condicional); D6 la cuenta atrás se reiniciaba con los retenidos y contaba desde la caída más antigua; D8 módulo no dado de alta ahora deja incidencia; D10 el coordinador que importa es el de la partida; D11 se valida que el plan de ronda referencie módulos reales por slug; D12 un impacto cuenta como señal de vida. **D9 (`133d760`) — desplegado y dictaminado CONFORME CON OBSERVACIONES en la 4ª supervisión, tras tres NO CONFORME corregidos:** barrido de obsolescencia. La caída dependía por completo de que llegara el Last Will, y ese mensaje se pierde si el broker se reinicia sin persistencia o si la sesión cae sucia: `online` se quedaba pegado a `true` y un módulo muerto pasaba por vivo para siempre. Ahora `findStaleModules` (dominio puro) + `ResilienceService.sweepStale()` cada 15 s (`RESILIENCE_SWEEP_MS`, 0 desactiva) dan por caído a quien lleve más de 90 s callado (`STALE_AFTER_MS`; un módulo vivo habla cada segundo por telemetría). Entra por el MISMO `record()` que un LWT, así que auto-pausa, incidencias y decisión del operador se comportan igual. *(La redacción original decía que la caída se fechaba en la última señal de vida; el D3 de la 1ª supervisión lo invirtió: `offlineSince` marca el instante de la **declaración** y `lastSeenAt` conserva el silencio real. Se corrige aquí para que la ficha no se contradiga a sí misma.)* Incidencia `module_stale` distinguible (se dedujo del silencio, no se recibió el LWT). En pantalla: antigüedad del silencio por módulo y aviso «Módulo sin señal» adelantándose al barrido. **1ª supervisión NO CONFORME (4 bloqueantes), corregidos:** D1 el barrido no distinguía «el módulo calla» de «el backend está sordo» — con el broker caído habría pausado una ronda viva sin poder ni enviar la orden (ahora no barre sin escucha, ni recién reconectado, y el silencio simultáneo de todos se registra como `presence_blackout` sin declarar caídas); D2 un módulo declarado muerto por silencio no resucitaba jamás porque `touch` exigía `online: true` y la presencia sólo se publica al reconectar el MQTT — quedaba ausente para siempre mientras seguía disparando (ahora la telemetría y los impactos, que no son retenidos, lo devuelven a la vida; `status`, que sí lo es, no); D3 la cuenta atrás nacía agotada SIEMPRE, porque el silencio tolerado (90 s) supera la ventana (60 s) — la caída peor sustentada daba cero margen y la mejor 60 s (ahora `offlineSince` marca la declaración y `lastSeenAt` conserva el silencio real); D4 tres piezas nuevas se podían borrar con las pruebas en verde (el temporizador no estaba probado que barriera, ni `statusOf` que publicara los callados, ni el `unref`). **Verificado con mutación:** 7 mutaciones aplicadas sobre el código real, las 7 mueren. Observaciones N2-N6 corregidas. **2ª supervisión, otra vez NO CONFORME (3 bloqueantes), corregidos:** N-D1 la pantalla prometía «la ronda se pausará sola» justo en los dos casos que el guardarraíl acababa de crear —sin escucha del broker y en apagón— (ahora `statusOf` publica `sweep {listening, blackout}` y el texto dice la verdad en cada caso); N-D2 la incidencia `presence_blackout` se emitía en cada barrido, ~5.760 al día, ahogando el registro donde el operador tenía que ver justo eso (ahora una por apagón); N-D3 tres mutaciones sobrevivían sobre el cableado nuevo de D1/D2 — el `revives` de la ingesta se podía invertir (resucitar con el retenido y no con la telemetría) con las 446 pruebas en verde. Añadidas pruebas de `MqttService` real y del cableado; los manejadores `close`/`offline` resultaron ser código muerto y se eliminaron en vez de fabricarles cobertura. **Punto ciego acotado:** el apagón sólo se tolera 4 min (`BLACKOUT_GRACE_MS`); pasados, se declaran las caídas igualmente, porque quedarse ciego para siempre era peor. **3ª supervisión, NO CONFORME otra vez (2 bloqueantes + 9 mutaciones supervivientes), corregidos:** B1 el plazo de tolerancia del apagón NO se reiniciaba al perder el broker, así que el rato sordos se acumulaba como silencio de los módulos y con 91 s de escucha se habrían declarado las caídas de todos — la confusión sordera/caída colada por el reloj, reproducida ejecutando por el supervisor; B2 con el barrido desactivado (`=0`, configuración soportada) la pantalla seguía prometiendo la pausa automática (cuarto camino de la misma mentira; ahora `sweep.enabled`). Y 9 mutaciones sobre el código nuevo sobrevivían, incluidas las que ponían `BLACKOUT_GRACE_MS` en 40 min o en 0 —la prueba derivaba el instante de la propia constante y se movía con ella—: plazo fijado ahora con literal. **Sobre la mutación, con una corrección importante:** el organizador declaró «12 aplicadas, 12 mueren», y esa afirmación NO se reproduce. La 4ª supervisión repitió la campaña con 25 mutaciones (17 backend + 8 frontend): **23 mueren y 2 sobrevivían**. Una era real: borrar el reinicio del plazo en la salida «recién reconectado» reintroducía B1 con las 493 pruebas en verde — la batería del organizador sólo había mutado el OTRO reinicio y dio el caso por cubierto. Corregido en `70b2590` con la prueba del corte breve de broker (verificado: el mutante ahora muere). La otra superviviente (frontera exacta `heldMs === 240000`) es casi equivalente y se declara sin cerrar. Lección aplicada: una campaña de mutación necesita un control neutro que confirme que la línea base es válida. Corregido también el texto de §6.9, que era más benévolo que la realidad: «se reanuda con un botón» no vale si entre los caídos está el coordinador (pausa dura, única salida abortar). **4ª supervisión: CONFORME CON OBSERVACIONES, sin bloqueantes nuevos.** Verificó en vivo (sólo lectura) que la imagen desplegada contiene el código y que el arranque es limpio, con el barrido oyendo al broker. Observaciones corregidas en `70b2590`: la pantalla seguía diciendo «no se declara ninguna caída de momento» pasado el plazo del apagón, mientras el barrido ya las declaraba; el texto prometía «unos segundos» cuando el intervalo es configurable (ahora se publica `intervalMs` y se dice el valor real); y los comentarios del código conservaban la frase que la documentación ya había matizado. **Declarado sin cerrar:** N7 (dos réplicas del backend barrerían a la vez; hoy `compose.yml` no declara réplicas); el reinicio del backend en mitad de un apagón reinicia el plazo, porque ese estado vive en memoria; y —límite nuevo, escrito en el dominio— con un broker en **bucle de reinicio** el plazo se reinicia siempre y la cota de 4 min deja de existir: no se declara ninguna caída. Es el lado conservador (nunca una pausa falsa), pero el sistema se queda ciego en ese régimen. |

## Programa «Panel usable» (F1–F6)

Requisitos de producto en `docs/product/alcance-panel-roles-firmware.md`. Modelo: admin=fabricante;
vender módulo=vincularlo=convierte al comprador en gestor; todo usuario es jugador; estadística
del registrado global; temporal por partida sin cuenta.

| Fase | Alcance | Estado |
|---|---|---|
| **F1** | Cimientos: roles jugador/gestor en RBAC + login real (JWT) + sesión por rol + nav filtrada + datos demo | ✅ **CONFORME** (supervisor 2 vueltas). Desplegada y verificada en VM109 (`develop` @ `bd0254e`). Backend 172, frontend 43 |
| F2 | Propiedad de módulos (`Module.owner`, vincular/desvincular; gestor cede, admin reasigna) | ✅ **CONFORME CON OBSERVACIONES** (supervisor). Desplegada y verificada en vivo (`develop` @ `d5120d9`); migración `owner_id` en BD viva; backend 179, frontend 45. Flujo: link→gestor, unlink→jugador. Observaciones (no bloqueantes, para F5): (1) `link` con update condicional `where ownerId:null` (evitar TOCTOU); (2) degradación bajo `unlink` concurrente; (3) el canje por un jugador necesitará permiso/endpoint propio |
| F3 | Firmware/OTA (admin sube, gestor acepta, versión por módulo) | ✅ **CONFORME CON OBSERVACIONES** (supervisor independiente reejecutó todo: backend 188/tsc limpio, frontend 48/tsc/lint limpio; 7/7 reglas cubiertas por código+test; verificado en vivo). D1 (`:moduleId` sin validar→500) **corregido** con `ParseUUIDPipe` y verificado en vivo (malformado→400). Difieren a F5 (requieren migración): D2 carrera de "un despliegue en curso" (índice parcial único) y D3 compatibilidad de placa `targetBoard`↔módulo (campo `Module.targetBoard` explícito; forzarlo sobre `hardwareRevision` es ambiguo y podría bloquear despliegues legítimos). Observación de producción: con `MQTT_ENABLED` off, `sendOtaCommand` descarta en silencio y el `Deployment` quedaría en `sent` sin OTA real. — Detalle abajo:** Desplegada en VM109 (`develop`, backend rebuild + force-recreate). Endpoints `/api/modules/:id/firmware/{available,deploy,deployments}`; aceptar versión firmada → `Deployment` + OTA real por MQTT (`sendOtaCommand`, valida esquema + exige firma; publica por `slug`). Subida sigue en CRUD `POST /api/firmware` (`firmware:write`, admin). Backend 188 (+9), frontend 48 (+3). Verificado en vivo: deploy→`sent`+`commandId`; 2º deploy en curso→409; sin firma→400; no-dueño→403. Fix en vivo: `DeployFirmwareDto` sin decoradores class-validator daba 400 (cazado por verificación viva, no por unit tests). Estado demo: módulo `demo-diana-01` @1.0.0 + versión firmada `1.2.0` desplegable |
| F4 | Jugadores + temporales por partida + reset de estadística por partida + vista del jugador | 🟡 **COMPLETA EN EL REPOSITORIO, SIN DESPLEGAR Y SIN REVISIÓN INDEPENDIENTE DE LAS CORRECCIONES.** Lo anterior (búsqueda de jugadores, alta de plantilla, equipos, **temporales por partida** con identidad XOR `playerId`/`guestName`, unión por QR, ficha/histórico en el marcador) sí está desplegado. **El reinicio de estadística NO.** Implementado en `85403db`: `POST /api/statistics/games/:gameId/participants/:participantId/reset` (`stats:reset`: gestor de sus paneles + admin; el rol jugador NO lo tiene), auditado (`stats.reset`, marca `selfReset`) e idempotente; borra `Result`+`Penalty`+`ShotCount` del jugador en esa partida y **desatribuye** sus `HitEvent` en vez de borrarlos, porque son telemetría del firmware; rechaza con la partida `running`/`paused`. **1ª supervisión: `NO CONFORME`, 3 bloqueantes, dos de ellos graves; corregidos en `4ff69ec` y PENDIENTES DE REVISIÓN INDEPENDIENTE:** **B1** · la premisa «nadie escribe la tabla `Statistic`» **era FALSA** —el grep que la sostenía sólo miró `backend/src` y el escritor está en `server/worker/src/tasks.ts` (`recomputePlayerStatistics`, cada 5 min)—, así que el reinicio dejaba intactos los totales globales y, al reiniciar la única partida de un jugador, el `if (results.length === 0) continue` del worker le congelaba los totales **para siempre**; **B2** · en una partida de UN SOLO jugador —el modo normal del producto— el reinicio **no se veía**, porque `buildRanking` re-infiere la atribución cuando hay un único participante y le readjudicaba los impactos desatribuidos (resuelto distinguiendo «nunca se pudo atribuir» de «se apartó a propósito» con `HitEvent.statsResetAt`, migración aditiva); **B3** · se borraba el `Result` ANTES de desatribuir los impactos, así que un `compute` concurrente lo recreaba intacto, y el estado de la partida se leía fuera de la transacción (TOCTOU). **Migración sin aplicar en producción: `20260726210000_hit_stats_reset`.** No verificado contra PostgreSQL real ni contra la VM 109 |
| F5 | Ascenso a gestor por venta (código de activación por correo, regenerable; SMTP pendiente) | 🟡 **CONSTRUIDA EN EL REPOSITORIO, SIN DESPLEGAR Y SIN REVISIÓN INDEPENDIENTE DE LAS CORRECCIONES.** *(La frase anterior de esta fila —«Sin hacer: el código de activación específico del ascenso a gestor»— era cierta el 26 de julio y hoy es FALSA.)* Implementado en `afe3e82`: modelo `ManagerActivation` + migración aditiva y re-ejecutable (código único, estado, caducidad de 24 h, nota de entrega, emisor, instante de activación). **Hallazgo al abrirlo:** el código no estaba «a medias», **no existía** — `link()` ascendía a gestor en la misma transacción que asignaba el dueño, así que el comprador se encontraba permisos que nunca había aceptado. Ahora `link()` **abre** el ascenso y no asciende; `POST /api/manager-activations/activate` lo usa el propio comprador; un código ajeno da el MISMO error que uno inexistente (probar códigos no confirma cuáles existen); activar exige seguir poseyendo un módulo; desvincular el último degrada a jugador **y** revoca los códigos pendientes. El alfabeto evita I, O, 0 y 1 porque hoy hay que dictarlo por teléfono. **1ª supervisión: `NO CONFORME`, 4 bloqueantes; corregidos en `1aa1fbc` y PENDIENTES DE REVISIÓN INDEPENDIENTE:** **B4 (seguridad, el más serio de la tanda)** · el rol y los permisos salían del **token**, congelados hasta 8 h — un ex-gestor degradado conservaba sus permisos toda la vida del token (revocarle los códigos no servía de nada) y un recién ascendido veía menús de gestor mientras el backend le respondía 403; ahora `JwtStrategy.validate()` lee el rol **vigente de la base en cada petición** y rechaza cuentas desactivadas (verificado en `server/backend/src/modules/auth/jwt.strategy.ts:41-56`); **B2** · activar con un rol que no fuera jugador quemaba el código sin ascender a nadie y dejaba un callejón sin salida; **B3** · `link()` había perdido la transaccionalidad y el reintento no reparaba; **B1** · la pantalla de activación **no era alcanzable** desde el menú (sólo tecleando la URL), es decir, el paso 3 del §3.1 no se podía ejercer desde el panel. **SMTP sigue sin configurar y no se finge lo contrario:** sin relay la nota de entrega dice que NO se ha enviado nada y el código se le muestra al admin para que lo dicte. **Migración sin aplicar en producción: `20260726200000_manager_activation`.** **Aviso sobre las pruebas:** las 8 pruebas en rojo de hoy son de esta fase (bomba de relojería por fechas fijas, ver cabecera); no invalidan el código, pero dejan la suite en rojo |
| F6 | Diagnóstico (test de impacto y LEDs desde el panel) | 🟡 **CABLEADA AL BACKEND REAL EN EL REPOSITORIO, SIN DESPLEGAR, SIN REVISIÓN INDEPENDIENTE DE LAS CORRECCIONES Y SIN EJERCER NUNCA EL BUCLE COMPLETO.** *(La frase anterior de esta fila —«el backend no expone `test-led`, `test-sensor`, `calibrate` ni `commands/identify`»— era cierta el 26 de julio y hoy es FALSA.)* Verificado hoy en `server/backend/src/modules/modules/module-diagnostics.controller.ts`: `@Controller('modules')` expone `POST :idOrSlug/commands/identify` (l. 54), `POST :idOrSlug/targets/:targetIndex/test-led` (l. 77), `.../test-sensor` (l. 102), `.../calibrate` (l. 121), `POST :idOrSlug/commands/abort-calibration` (l. 140) y `GET :idOrSlug/diagnostics` (l. 158) — exactamente las rutas que `realAdapter.ts:81-116` llamaba al vacío. Acotado por propiedad (un gestor no enciende los LED de un módulo ajeno: **404, no 403**). **Dos decisiones de honestidad que cambian el contrato del panel:** (1) ordenar una prueba **no es** conocer su resultado — los comandos van por MQTT y el resultado se lee en `GET .../diagnostics`, en vez de inventar una medida inmediata; (2) **no hay prueba de sensor por diana en el contrato v1**, así que se pide `self_test` (alcance = módulo entero) y la respuesta lo dice. Además se corrigió que los diagnósticos que llegaban por MQTT **se validaban y se tiraban** (nadie los guardaba: el resultado de una prueba no se podía consultar jamás; ahora se persisten como incidencias con `source: 'diagnostic'`). **1ª supervisión: `NO CONFORME`, 3 bloqueantes; corregidos en `1aa1fbc` y PENDIENTES DE REVISIÓN INDEPENDIENTE:** **B1** · los comandos **violaban nuestro propio contrato** — `led_test` y `start_calibration` llevaban parámetros (`target_index`, `pattern`) que el esquema no admite (`additionalProperties: false`), así que el validador de salida tumbaba la publicación y **la prueba de LED y la calibración no salían nunca**; **B2** · `TestLedsPage` pintaba la diana encendida ANTES del `await` y sin `catch`, o sea la misma pantalla de mentira que el bloque decía eliminar, sólo corregida en la de sensores; **B3** · las dos correcciones que el commit presentaba como hallazgos propios (persistir los diagnósticos y `delivered`) **no tenían ni una prueba** y se podían borrar enteras con la suite en verde. **Declarado sin cerrar por el propio autor:** el bucle completo (ordenar → el módulo publica → el operador lee) **no se ha ejercido en ninguna capa**, ni siquiera contra el simulador del repo, que trata `led_test` como no-op y no publica resultados de calibración. Nada de esto se ha probado contra un módulo real |

Observaciones no bloqueantes del supervisor para fases posteriores: `gestor` con `statistics:read`
global y nav Resultados/Estadísticas visibles al jugador → acotar con el auto-scoping por dueño (F2/F4).
**Sigue abierta a 2026-07-26.** El acotado por dueño ha avanzado mucho (módulos, presets,
matrices, vistas y paneles se acotan ya por propiedad), pero **no consta que se haya acotado
`statistics:read` del gestor**; en G-G sólo se comprobó que el enlace del menú exige el permiso,
que es otra cosa.

## Bitácora

- **2026-08-05** · **DESPLEGADO en la VM 109: `develop` @ `6da16d4`** (antes `133d760`, 17 commits).
  X-06, D9, F4, F5, F6, los ocho bloqueantes de las re-supervisiones, el arreglo del worker y el
  cableado de las tres pantallas de diagnóstico. **3 migraciones aplicadas** y verificadas contra
  la base viva (`stats_reset_at`, `manager_activations`, `module_slug`/`device_occurred_at`).
  Copia de la base tomada y verificada ANTES de tocar nada. 7/7 contenedores `healthy`.
  **El worker vuelve a ejecutar sus tareas**: `recomputePlayerStatistics` y las tres de retención
  corren sin un solo error, donde llevaba semanas en bucle de fallos. Motor de Prisma comprobado
  DENTRO de la imagen antes de levantar: `debian-openssl-3.0.x`.
  **Dos fallos reales cazados durante el despliegue, ambos del tipo «funciona en el repositorio,
  falla en la imagen»:** (a) el Dockerfile del worker no copiaba `tsconfig.build.json`, añadido
  al arreglar el healthcheck: la compilación se cayó en seco (`7372bab`); (b) **el healthcheck
  descartaba TODOS los latidos** —el validador seguía exigiendo el campo anterior al cambio a
  contadores por tarea— y declaraba enfermo un worker perfectamente sano; `readHeartbeat` no
  tenía ninguna prueba y el módulo ejecutaba su `main()` al importarse, que era la razón de fondo
  de que no la tuviera (`6da16d4`, 20 pruebas, verificado por mutación).
  **Lo que este despliegue NO resuelve:** quedan **11 pantallas** en el adaptador de
  demostración (`stats`, `home`, `users`, `system`, `topology`, `new-game`, `incidents`,
  `countdown`, `module-detail`, `backups`, `results`) — es el resto vivo de X-21. Y sigue sin
  verificarse la ingesta de extremo a extremo (X-18-INGESTA): no hay hardware ni simulador
  conectado al broker de la VM.
- **2026-08-04** · **Worker de producción arreglado en el repositorio (no desplegado) y F6 ejercible de
  extremo a extremo.** (a) **El worker mentía dos veces.** El contenedor se declaraba `healthy`
  mientras todas sus tareas fallaban en bucle. Causa demostrada **en los binarios** (no por
  lectura): la imagen `diana/worker:local` desplegada contiene sólo
  `libquery_engine-debian-openssl-1.1.x.so.node` sobre un sistema Debian 12 con **OpenSSL
  3.0.20**, mientras `diana/backend:local` sí lleva el `3.0.x` correcto. La etapa `build` del
  Dockerfile generaba el cliente sin `openssl` instalado y Prisma cae entonces en un valor fijo
  por defecto. Corregido declarando `binaryTargets` **en el esquema**, que no depende de qué
  imagen base tenga cada etapa. El healthcheck pasa de `pgrep` (proceso vivo) a latido en disco
  con comprobación real. (b) **La revisión independiente del arreglo encontró un tercer
  defecto**: el healthcheck nuevo usaba un contador de fallos ÚNICO para todas las tareas, así
  que el éxito de `statistics` (cada 5 min) borraba los fallos de `retention` (cada 24 h) y la
  purga de datos podía llevar semanas rota sin detectarse. Cuentas por tarea. (c) **F6**: el
  simulador declaraba el tópico de diagnóstico y **no publicaba en él jamás**, así que la cadena
  ordenar→responder→persistir→mostrar no se había ejercido nunca; ahora responde y hay una
  prueba que la recorre entera contra el contrato congelado. El LED contesta como
  `self_test_result` con `detail.component`, porque el contrato **no admite** `led_test_result`.
  (d) Cerradas además: fallo de persistencia de diagnósticos que se tragaba en silencio,
  diagnóstico de módulo sin dar de alta invisible, hora mostrada = ingesta en vez de suceso, y
  en F4 el borrado del acumulado global sin filtrar `scope`. Commits `0343a76` y `36f8e14`.
  Suites: backend 639 (7 saltadas), frontend 181, worker 15, simulador 34, contratos 43/0.
  **Acceso SSH a la VM: nunca estuvo roto**, el usuario es `diana-admin` (no `diana`), tal como
  ya decía `deployment/procedimiento.md`; no se cambió nada en la máquina.
  **Sin verificar todavía:** que la imagen reconstruida traiga el motor `3.0.x` (no hay acceso
  al demonio de Docker desde ia02 ni desde los agentes; se comprueba al reconstruir, ANTES de
  levantar), y el comportamiento del healthcheck dentro de un contenedor real.
- **2026-08-04** · **Re-supervisión independiente de F4, F5 y F6: las tres `NO CONFORME`.**
  Ocho bloqueantes nuevos, todos de la misma clase: el arreglo estaba en el código y era
  correcto, pero **nada lo fijaba**. F5: la suite llevaba en rojo desde el 28 de julio por
  fechas absolutas (8 fallos), y la corrección de seguridad —leer el rol de la base en vez del
  token— se revertía con dos líneas sin que fallara una sola prueba. F4: la marca del reinicio,
  el orden apartar→borrar y la recomprobación dentro de la transacción, todos borrables en
  verde; y la pantalla seguía contando los impactos apartados en sus totales, contradiciendo lo
  que el propio diálogo promete al operador. F6: ninguna de las seis rutas tenía su permiso
  fijado —y el guard es **permisivo por defecto**, así que borrar un decorador abría la
  calibración a cualquier autenticado—, y las dos pruebas llamadas «la diana NO se pinta» sólo
  miraban el texto del error, no la rejilla. Corregido en `0343a76`, verificado por mutación
  con control neutro: los doce mutantes supervivientes mueren. Se cerró además una ventana
  equivalente en el canal en directo (un WebSocket dura horas: desactivar una cuenta la echaba
  del REST y le dejaba el directo abierto). Suites: backend 630, frontend 179, `tsc` limpio
  incluido el árbol de pruebas, que **no compilaba**. **Las correcciones de `0343a76` siguen
  sin revisión independiente.**
- **2026-07-20** · Auditados ambos repositorios y el nodo Proxmox. `Diana` contenía sólo el
  dosier y un README. `s9-server` accesible, con convención de ficha por máquina y por servicio.
- **2026-07-20** · Inventario Proxmox: VMID 109 e IP .209 libres; `vmbr0`; thin pool con 211 GB.
  Nodo sobrecomprometido en RAM (37,8 GB asignados sobre 32 GB, 4,4 GB de swap en uso).
- **2026-07-20** · Contratos MQTT v1 congelados y puestos a revisión.
- **2026-07-20** · Lanzada la Ola 1 con siete paquetes en worktrees aislados.
- **2026-07-20** · VM 109 creada. **Incidencia:** la imagen `debian-12-genericcloud` traía
  cloud-init instalado pero no se ejecutó, así que la VM arrancaba sin red y con `ssh.service`
  caído por ausencia de claves de host. Resuelto inyectando red estática, claves de host y
  clave pública en el disco montado por `kpartx`, y desactivando la gestión de red de
  cloud-init. Verificado: ping 3/3 y SSH por clave.
- **2026-07-20** · Dictamen del supervisor sobre la Ola 0: **`NO CONFORME`**, con dos
  bloqueantes (ACL contradictoria que dejaba inejecutable la consolidación temporal; `$ref`
  que sólo resolvían con el validador propio) y cinco mayores. Todos corregidos y publicados.
- **2026-07-26** · **Cierre de deudas declaradas** (`e4a9de3`, `ade9a21`), antes de desplegar.
  (a) **Atribución impacto→jugador**: el impacto MQTT no dice quién disparó, así que sólo se
  atribuye cuando la respuesta es forzosa (un único participante en la ronda, o cada jugador en
  su propio panel). Con varios jugadores compartiendo panel **se deja sin atribuir con el
  motivo escrito, no se adivina**. Era lo que dejaba el marcador multijugador a cero. (b) El
  `D9` del supervisor de G-H: `/topology/panels` no acotaba por dueño. (c) Contrato
  `coordinator_module_id` en `module-config` (aditivo) + composición y publicación de la
  configuración deseada, dejando dicho que **publicar el deseo no es aplicarlo**. (d) F3·D3
  compatibilidad de placa: sin placa declarada se **rechaza**, no se supone compatible. (e)
  F3·D2 «un despliegue OTA en vuelo» pasa de comprobación con carrera a **índice único parcial**.
- **2026-07-26** · **Segundo despliegue del día: D9 (`133d760`) en la VM 109.** Imágenes de backend y frontend reconstruidas; verificado que el artefacto contiene el código (`sweepStale`, `presence_blackout`, `BLACKOUT_GRACE_MS`) ANTES de levantar, stack sano y API/panel a 200. **Incidencias del despliegue:** (a) dos intentos de lanzar la construcción en segundo plano parecieron fallar por tiempo pero SÍ arrancaron en la VM, dejando tres builds compitiendo por la memoria — hubo que matarlos; (b) con ~1,5-2 GB efectivos hay que **parar contenedores para compilar** (corte de servicio durante el despliegue), que es el procedimiento §8. **No se pudo probar el barrido en vivo:** la escritura de prueba en la base (poner un módulo en línea con una señal antigua para verlo caer) fue bloqueada por el clasificador de seguridad y no se forzó. Sí quedó demostrado en vivo que llega presencia retenida real de `module-01/02/03` y que el backend deja incidencia por no estar dados de alta.
- **2026-07-26** · **4ª supervisión de D9: CONFORME CON OBSERVACIONES.** Sin bloqueantes nuevos. Corrigió una afirmación del organizador que no se reproducía («12 mutaciones, 12 mueren»): con 25 mutaciones sobrevivían 2, una de ellas real (reintroducía B1 con toda la suite en verde). Observaciones corregidas en `70b2590`.
- **2026-07-26** · **Lote G-A…G-I desplegado en la VM 109** (`develop` @ `045fdd1`, commit
  `8220a45`). 4 migraciones aplicadas contra la base viva, imágenes reconstruidas, 8/8
  contenedores `healthy`, copia de la base tomada **antes** de tocar nada. La verificación en
  vivo fue de superficie: rutas nuevas devolviendo 401 (existen y exigen auth, contrastado con
  el 404 de una ruta inexistente) y esquema real con las tablas, columnas e índice esperados.
  **N8 cerrado con evidencia:** la prueba de concurrencia del cerrojo de panel se ejecutó por
  fin contra PostgreSQL real; el **primer intento falló** y destapó un defecto de la propia
  prueba (creaba ambas partidas en `armed`, estado que ya ocupa panel, así que se rechazaban
  mutuamente sin ejercitar el cerrojo). Corregido y reejecutado. **Nadie ha entrado al panel
  con credenciales reales:** la verificación funcional queda para el operador.
- **2026-07-26** · **D9 (barrido de obsolescencia de G-I)**, `133d760`. Es el agujero de fondo
  de la detección de caídas: dependía por completo del Last Will, que **puede no llegar nunca**
  (broker reiniciado sin persistencia, sesión caída sucia), y entonces `online` se quedaba
  pegado a `true` y un módulo muerto pasaba por vivo indefinidamente. **Tres supervisiones
  independientes seguidas dictaminaron `NO CONFORME`** —1ª: 4 bloqueantes; 2ª: 3 bloqueantes;
  3ª: 2 bloqueantes + 9 mutaciones supervivientes— y las tres tandas se corrigieron. El defecto
  que más veces reapareció con caras distintas fue el mismo: **confundir «el módulo calla» con
  «el backend está sordo»**, y prometer en pantalla una pausa automática que no iba a ocurrir.
  Verificación final por mutación: 12 mutaciones (8 backend + 4 frontend), 12 mueren. **La 4ª
  supervisión está EN CURSO al cerrar esta entrada: D9 no está aprobado ni desplegado.**
  *(Nota añadida el 2026-08-04: se conserva el texto tal como se escribió ese día, pero ambas
  cosas se resolvieron después — la 4ª supervisión dictaminó `CONFORME CON OBSERVACIONES` y D9
  (`133d760`) **es** el commit que hoy corre en la VM 109.)*
- **2026-07-26** · **Incidencia de infraestructura · DNS de la VM 109 roto** (preexistente, no
  causada por el despliegue). El único resolver de la VM era el MagicDNS de Tailscale
  (`100.100.100.100`), que respondía `server misbehaving`: `git`, `docker pull` y `npm ci`
  fallaban todos, y cualquier build o actualización estaba bloqueada. Se desplegó sin tocar la
  red de forma permanente (código por **bundle de git** vía SSH; override **temporal** de
  `/etc/resolv.conf` al router, con copia previa, restaurado y comprobado al terminar). **La
  causa de fondo sigue sin resolver:** exige decisión del operador
  (`tailscale set --accept-dns=false` o revisar los nameservers de la tailnet). Detalle en
  `docs/deployment/procedimiento.md` §8.
- **2026-07-26** · **Incidencia de infraestructura · memoria de la VM 109.** La VM está
  declarada con `memory=4096` + `balloon=1024` (D-11) sobre un nodo Proxmox
  **sobrecomprometido** (D-11: 37,8 GB asignados sobre 32 GB físicos), así que el globo le
  quita memoria y **no dispone de los 4 GB nominales**. Consecuencia medida y repetida durante
  G-C/G-F: **BuildKit muere por OOM a mitad de compilar y deja la imagen anterior en su sitio**,
  con el síntoma engañoso de un `build` que «termina» y un `dist/` sin el cambio. El
  procedimiento fiable (parar contenedores no esenciales, compilar, **verificar el artefacto**
  y sólo entonces levantar) está en `docs/deployment/procedimiento.md` §8. La cifra exacta de
  RAM efectiva **no está verificada en este repositorio**: `procedimiento.md` §8 anotó «~1 GB»
  al observarlo; no se ha vuelto a medir desde aquí, y este documento no toca la VM.
- **2026-07-26** · Barrido de documentación (este commit): se reconstruyó el estado real por
  bloque leyendo el historial de git, porque varias filas seguían diciendo «supervisor en
  curso» o «SIN DESPLEGAR» de trabajo ya supervisado y desplegado. Contradicciones resueltas y
  colisión de identificadores `X-18` documentada más arriba.
- **2026-08-04** · **Barrido de documentación: la documentación se había quedado congelada en
  `133d760` (2026-07-26) y afirmaba cosas falsas.** Desde entonces habían entrado seis commits
  que ningún documento reflejaba: `5c3b7ac`+`eb42324` (X-06, vista en directo), `d42f474`+
  `1aa1fbc` (F6), `85403db`+`4ff69ec` (F4) y `afe3e82`+`1aa1fbc` (F5). Se ha reconstruido el
  estado leyendo los commits y el código, no la documentación previa.
  **Las tres afirmaciones de bulto que se corrigen** (ciertas el 26 de julio, falsas hoy):
  F6 «🔴 Pendiente contra el backend real», F5 «🟡 Parcial… sin hacer el código de activación»
  y F4 «el reinicio no está hecho». Las tres están hoy **construidas en el repositorio** —y
  ninguna de las tres, desplegada ni revisada.
  **Lo que este barrido deja escrito con todas las letras, porque confundirlo fue el error
  anterior:** (a) *en el repositorio* ≠ (b) *desplegado en la VM 109* ≠ (c) *probado por
  alguien*. La VM **sigue en `133d760`**; **nada** de X-06, F4, F5 ni F6 está desplegado;
  faltan por aplicar **dos migraciones** (`20260726200000_manager_activation` y
  `20260726210000_hit_stats_reset`); y las tres fases salieron **`NO CONFORME`** en su primera
  supervisión —diez bloqueantes entre las tres, uno de seguridad (el rol salía del token y
  quedaba obsoleto hasta 8 h)— con las correcciones **aún sin revisión independiente**.
  **Hallazgo propio del barrido, al reejecutar las suites:** el backend **no estaba en verde**.
  Daba 584 pasan / **8 fallan** / 7 saltadas, no los 592 que declaraba `1aa1fbc`. Las 8 eran de
  `test/invitations/manager-activation.spec.ts`: bomba de relojería en las propias pruebas
  (fechas absolutas fijadas al 2026-07-27 sin congelar el reloj), no un defecto del producto.
  El barrido lo documentó sin corregirlo, por no tocar código ni pruebas; **corregido después
  en `0343a76`** junto con los bloqueantes de las re-supervisiones.
  También se registra el **worker roto en producción** (`healthy` de mentira, todas las tareas
  fallando por desajuste del motor de Prisma) y se confirma que siguen sin resolver el DNS de
  la VM, la RAM efectiva y las pruebas de restauración y de `reboot`.
